# =============================================================================
# EduGuard Root Environment Configuration
# =============================================================================
# This is for reference only - actual .env files are in /server and /client

# See /server/.env.example for server configuration
# See /client/.env.example for client configuration
